# Web server
PORT=3000
HOTEL_NAME=Heaven's Door

# Email sender and admin
FROM_EMAIL="Heaven's Door <no-reply@example.com>"
ADMIN_EMAIL=admin@example.com

# SMTP (required for booking/contact emails)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
